Lambada表达式的标准格式： （由三部分组成）
    a:一些参数
    b:一个箭头
    c:一段代码
格式： （参数列表）->{业务逻辑};
解释说明：
    ():接口中抽象方法的参数列表
     ->:把参数传递给方法体
     {}: 方法体



(参数列表)：括号中参数列表的数据类型可以不写
(参数列表)：括号中的参数如果只有一个，那么类型和（）都可以省略
{一些代码}：如果{}中代码只有一行，无论是否有返回值，都可以省略括号，return 分号，要省略就要一起省略


Lambda的使用前提：
    1：使用Lambda必须具有接口，且要求接口中有且仅有一个抽象方法
    2：使用Lambda必须具有上下文推断
备注：有且只有一个抽象方法的接口，称作函数式接口