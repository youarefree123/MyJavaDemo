布局管理器：
    FlowLayout（流式布局管理器） Panel默认的布局管理器
    BorderLayout（边界布局管理器）Frame默认的布局管理器
    GridLayout（网格布局管理器）规则的矩阵（计算器）
    CardLayout（卡片布局管理器）选项卡
   GridBagLayout（网格包布局管理器）非规则的矩阵

事件监听机制：
    事件源（组件）
    事件（Event）
    监听器(Listener)
    事件处理(引发事件后的处理方式)

事件源：就是swing包或者awt包中那些封装好的组件
事件：每个事件源都有自己特有的对应事件和共性事件
监听器： 将可以触发某一个事件的动作（不止一个）都已经封装到了监听器中
！以上三者在java中已经定义好了，我们只需要对产生的动作进行处理

事件处理：由开发者自行编写
    
